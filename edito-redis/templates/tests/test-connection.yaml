apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edito-redis.fullname" . }}-test-connection"
  labels:
    {{- include "edito-redis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edito-redis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
