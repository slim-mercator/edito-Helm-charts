apiVersion: v1
kind: Service
metadata:
  name: {{ include "edito-chart.fullname" . }}
  labels:
    {{- include "edito-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edito-chart.selectorLabels" . | nindent 4 }}
