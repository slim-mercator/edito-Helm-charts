apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edito-chart.fullname" . }}-test-connection"
  labels:
    {{- include "edito-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edito-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
